commit 682b665a8a62ec526535bae358266f815abf4097
Author:     Sam Harwell <sam@tunnelvisionlabs.com>
AuthorDate: Wed Feb 22 08:40:04 2012 -0600
Commit:     Sam Harwell <sam@tunnelvisionlabs.com>
CommitDate: Wed Feb 22 12:25:55 2012 -0600

No need for static and/or private methods to be marked 'final'

diff --git a/tool/src/main/java/org/antlr/misc/BitSet.java b/tool/src/main/java/org/antlr/misc/BitSet.java
index da1f7cc..e293879 100644
--- a/tool/src/main/java/org/antlr/misc/BitSet.java
+++ b/tool/src/main/java/org/antlr/misc/BitSet.java
@@ -168,7 +168,7 @@ public class BitSet implements IntSet, Cloneable {
}
}

-    private final static long bitMask(int bitNumber) {
+    private static long bitMask(int bitNumber) {
int bitPosition = bitNumber & MOD_MASK; // bitNumber mod BITS
return 1L << bitPosition;
}
@@ -328,7 +328,7 @@ public class BitSet implements IntSet, Cloneable {
}
}

-    private final int numWordsToHold(int el) {
+    private int numWordsToHold(int el) {
return (el >> LOG_BITS) + 1;
}

@@ -584,7 +584,7 @@ public class BitSet implements IntSet, Cloneable {
return toString();
}

-    private final static int wordNumber(int bit) {
+    private static int wordNumber(int bit) {
return bit >> LOG_BITS; // bit / BITS
}
}

